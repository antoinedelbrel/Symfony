{% extends 'base.html.twig' %}

{% block title %}Modification d'un évenement{% endblock %}

{% block body %}

<div class="container">
    <h1>Modifier l'évenement: {{ event.name }}</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>

    {{ form_end(form) }}
</div>

{% endblock %}