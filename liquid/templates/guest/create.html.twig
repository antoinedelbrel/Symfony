{% extends 'base.html.twig' %}

{% block title %}Ajout d'un invité{% endblock %}
{% block body %}

<div class="container mt-4">
    <h1>Ajoutez un invité</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}
    {#<!-- <ul class="event" data-prototype="{{ form_widget(form.events.vars.prototype)|e('html_attr') }}">
        {% for event in formAdvert.events %}
        <li>{{ form_widget(form.events.var.prototype)|e('html_attr') }}</li>
        {% endfor %}
    </ul> -->#}





    <button type="submit" class="btn btn-primary">Ajoutez le nouvel invité</button>

    {{ form_end(form) }}
</div>

{% endblock %}